
- Completed FE-02 Conversation Thread: added ChatThread component with user/assistant messages, auto-scroll, msgIn animation, and empty→chat state transition. Tests: 9 passing (3 ChatThread + 6 page).
- Completed FE-03 Markdown Answer Rendering: added MarkdownContent component using react-markdown + remark-gfm with rich prose styles (brand-blue tables, tinted inline code, dark code blocks); wired into AssistantMessage in ChatThread. ESM transpilation handled via transpilePackages in next.config.ts. 18 tests passing. PR #13 open.
- Completed FE-04 SSE Streaming & Loading Indicator: useChat hook with AbortController, streamMessage stub (FE-08 swap-point), TypingIndicator (bouncing dots), send→stop button toggle in ChatInput, partial text preserved on stop/error. 34 tests. Also completed FE-03 Markdown Rendering (react-markdown, rich prose styles) earlier in this session.
- Completed FE-05 Session Conversation History: sessionIdRef via useRef(crypto.randomUUID()) in useChat, threaded through streamMessage signature, resetSession clears state and rotates ID
- Completed FE-06 New Conversation Control: ghost Button in ChatHeader with onNewConversation prop, wired to resetSession in page.tsx conversation branch only
- Completed FE-07 Entra Sign-in and Auth Gate: MsalProvider in Providers.tsx, AuthGate with SignInCard, useToken hook with silent→popup fallback, full auth gate wired into page.tsx
- Completed FE-08 Backend API Client — replaced stub streamMessage with real fetch+SSE client; Bearer token from useToken (FE-07); typed ApiError (auth/server/network) mapped to UI messages (auth → 'Session expired', else generic); getApiUrl from NEXT_PUBLIC_API_URL. Subagent-driven TDD, 69 tests green, tsc clean. Note: backend SSE framing (data: {token}) can split markdown paragraph breaks; handled with a defensive frontend parse, proper fix deferred to a backend story.
- Backend integration fixes (post-FE-08, frontend↔backend wired live): (1) JWT auth now accepts both v1/v2 issuers and bare + api:// audiences; (2) SSE tokens JSON-encoded so markdown newlines/tables survive the wire (proper fix for the FE-08 deferred note); (3) agent prompted to render structured data as Markdown tables; (4) **Person-column resolution** — Graph returns person cols (e.g. SME=SMEEmail) only as {Field}LookupId numeric ids; now resolved to {LookupValue, Email} via the hidden User Information List (cached); (5) person columns can't be OData-filtered (filter client-side on LookupValue) + 'Prefer: HonorNonIndexedQueries…' header for non-indexed cols; (6) datetime filters must be quoted ISO ranges; (7) **timezone-aware date filtering** — new filter_by_date tool converts local YYYY-MM-DD → UTC half-open range via configurable SITE_TIMEZONE (default Europe/London, DST-aware); (8) added scripts/fetch_schema.py generating src/app/data/sharepoint_schema.md injected into the system prompt. Merged to main via PR #24 + #25; 124 backend tests green, ruff clean. Housekeeping: deleted all 15 merged local+remote branches — only main remains.