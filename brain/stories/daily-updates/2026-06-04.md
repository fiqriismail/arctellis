
- Completed BE-00 & FE-00 Monorepo Setup — scaffolded Python 3.13 FastAPI backend (uv, ruff, pytest, Dockerfile) and Next.js 16.2.7 frontend (shadcn/ui v4 light theme, Tailwind v4) in a plain apps/ monorepo. TDD health endpoint passing. Both Story Boards updated.
- Completed BE-01 Entra App Registration & Graph Auth — added pydantic-settings config module, GraphAuthService (client-credentials via azure-identity), and verification script. 12 tests passing (mocked). Verification script execution deferred until Azure credentials are provisioned. Branch: feature/BE-01-graph-auth pushed to origin.
- BE-01 verification passed — token acquired and Graph site access confirmed (Procurement site). App registration using Sites.Read.All with admin consent.
- Completed BE-02 SharePoint Service (Read Items & Schema). Built SharePointService with get_schema() and get_items() backed by Microsoft Graph. Verified against real Procurement list: 75 columns, 22 items. Factory function resolves site ID from URL. 40 unit tests passing.
- Completed BE-03 SharePoint Data Caching. Added _TTLCache (dict + monotonic timestamps), wired into SharePointService with configurable cache_ttl param. get_schema() cached under key 'schema', get_items() cached under filter-aware 'items:{filter}' keys. Factory reads settings.cache_ttl_seconds. 54 unit tests passing.
- Completed BE-04 List Data Tools. Built make_tools(service) factory with 6 @tool closures: get_schema, filter_rows, count_rows, sum_column, average_column, group_and_aggregate. All numeric computation in Python. _parse_number helper skips unparseable rows. Import path langchain_core.tools confirmed. 80 unit tests passing.
- Completed BE-05 LangChain Agent + OpenAI Wiring. Built build_agent(service, settings) using create_agent() from langchain.agents with ChatOpenAI instance; invoke_agent() async helper with history support. SYSTEM_PROMPT guides model to call tools first, never fabricate, clarify ambiguity, decline unrelated questions. Verified end-to-end: schema listed, count returned, weather question declined. 91 unit tests passing.
- Completed BE-06 Conversation Endpoint + SSE Streaming. POST /chat accepts {question, session_id}, prepends per-session history, streams agent tokens via SSE (data: token

), appends history in try/finally so client disconnect is handled. main.py lifespan builds SharePointService + agent at startup. 101 tests passing.
- Completed BE-07 Entra Token Validation Middleware. validate_entra_token() uses PyJWKClient + RS256 decode against Entra JWKS. require_auth FastAPI dependency raises HTTP 401 for missing/invalid tokens (HTTPBearer auto_error=False). Wired at router level to protect all /chat routes. /health unprotected. 107 tests passing.
- Completed FE-01 Chat Layout & Centred Input: built ChatInput component (auto-resize via field-sizing-content, Enter submit, Shift+Enter newline) and centred page layout using shadcn Textarea + Button. Fixed Next.js Server/Client Component boundary by marking page.tsx as 'use client'. Branch feature/FE-01-chat-layout pushed.