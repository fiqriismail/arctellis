
- BE-13 Azure OpenAI migration (code complete, tag: review) — swapped ChatOpenAI → AzureChatOpenAI in agent.py; _build_llm() uses managed identity (DefaultAzureCredential bearer-token provider, Cognitive Services scope) by default with AZURE_OPENAI_API_KEY as local-dev fallback; config drops openai_* for azure_openai_endpoint/deployment/api_version/api_key; unit tests cover both auth paths, full backend suite green (128). No frontend story needed (no LLM coupling). Live streaming + Key Vault wiring deferred to BE-12.
- BE-13 Azure OpenAI migration pushed — PR #32 opened against main (https://github.com/arctellis/group-one-rtp/pull/32). Rebased onto latest main (origin had FE-11 styling + BE-08/09/10/11 merged that local main was missing); resolved agent.py conflict to keep both row_threshold make_tools and the _build_llm Azure swap. Backend 155 passed, frontend 81 passed. Story tag: review (live-streaming + Key Vault ACs land with BE-12).
- BE-13 PR #32 merged to main. Synced local main to origin/main (was 20 behind — pulled in FE-11 styling, BE-08/09/10/11, and BE-13). Cleaned up all 6 merged local feature branches. Local repo now clean on main (only the BE-10 basicConfig logging change remains uncommitted). Note: BE-10 observability shipped to main WITHOUT logging.basicConfig, so structured logs still won't surface on stdout — needs a follow-up fix.
- FE-12 Chart View implemented (tag: review) — Table/Chart toggle on assistant tables; bar/pie/donut via shadcn Chart (Recharts). Aggregation tables (one label + one numeric col, e.g. 'by status for estimated amounts') default to a pie chart; other chartable tables stay table-first. Pure TDD helpers: parseMarkdownTable, buildChartData ('Other' grouping + drop non-positive for pie), extractTableFromNode (hast). Frontend 113 tests pass, tsc + build clean. Branch feature/FE-12-chart-view (not yet pushed). No backend changes.
- FE-13 Sortable tables + currency/date formatting (tag: review) — assistant tables now render via a data-driven DataTable: click-to-sort columns (numeric/date/alphabetical), money columns as currency (€ default, explicit symbol preserved, 2dp, right-aligned), dates as dd-mm-yyyy, status badges retained. Column kinds inferred in parseMarkdownTable; pure formatCell helpers. Implemented on feature/FE-12-chart-view. Frontend 152 tests pass, tsc + build clean.
- Pushed feature/FE-12-chart-view and opened PR #33 (https://github.com/arctellis/group-one-rtp/pull/33): FE-12 charts (chart toggle now two-column only) + FE-13 sortable/formatted tables (currency €/2dp/right-align, dates dd-mm-yyyy) + RTP rebrand (group.one logo, 'RTP Intelligent Hub', procurement copy/prompts/placeholder) + BE-10 stdout logging fix. 152 FE tests green, build clean.