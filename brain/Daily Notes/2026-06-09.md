# Daily Note - 2026-06-09

- Completed [Ad-hoc] Extracted and reorganized Entra ID app registration instructions into a dedicated guide (`docs/guides/azure-entra-id-setup.md`).
- Completed [Ad-hoc] Fixed frontend build configuration to output to `build` instead of `.next` (`next.config.ts`).
- Completed [Ad-hoc] Updated frontend login page to use the `group_one.svg` logo and the "RTP Intelligent Hub" title (`SignInCard.tsx`).
